/**
 * @file
 * Behaviors for the covid19 theme.
 */

(function($, _, Drupal) {
  Drupal.behaviors.covid19 = {
    attach: function() {
      // covid19 JavaScript behaviors goes here.
    }
  };
})(window.jQuery, window._, window.Drupal);
